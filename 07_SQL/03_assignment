--Third assignment of SQL module by patika.dev https://app.patika.dev/courses/sql/Odev3
--1. in the country table, list all the countries that their name starts with 'A' and ends with 'a'
--SELECT country FROM country
--WHERE country LIKE 'A%a';

--2. in the country table, list all the countries that their name contains at least 6 characters and the last character is 'n'
--SELECT country FROM countryS
--WHERE country LIKE '_____%n';

--3. in the film table, sort all the movie titles that contains at least 4 'T' or 't'
--SELECT title FROM film
--WHERE title ILIKE '%t%t%t%t%'

--4. in the film table, sort all the data in columns that their title starts with 'C', their length is greater than 90 and the rental_rate = 2.99
SELECT * FROM film
WHERE title LIKE 'C%' AND length > 90 AND rental_rate = 2.99;


