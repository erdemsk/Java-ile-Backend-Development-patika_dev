-- Fifth assignment of SQL module by patika.dev https://app.patika.dev/courses/sql/Odev5
--1. List the 5 longest movies in the film table that their title ends with the 'n'.
--SELECT * FROM film
--WHERE title ILIKE '%n'
--ORDER BY length DESC
--LIMIT 10;

--2. List the second shortest 5 movies (6., 7., 8., 9., 10. shortest) that their title ends with 'n' in the film table
--SELECT * FROM film
--WHERE title LIKE '%n'
--ORDER BY length ASC
--OFFSET 5
--LIMIT 5;

--3. Sort the first 4 data with store_id 1 in descending order according to the last_name column in the customer table.
SELECT * FROM customer
WHERE store_id = 1
ORDER BY last_name DESC
LIMIT 4;
